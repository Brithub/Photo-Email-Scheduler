[project]
name = "photo-email-scheduler"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]",
    "pyyaml"
]

[dependency-groups]
dev = [
    "pytest",
]
